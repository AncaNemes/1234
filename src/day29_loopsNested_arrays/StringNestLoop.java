package day29_loopsNested_arrays;

public class StringNestLoop {
    public static void main(String[] args) {
        String word = "hello world";
        //find duplicates and count
        //find unique characters (appearing only once);
        //print character and number of occurance

        for (int w = 0; w < word.length() - 1; w++) {
            System.out.print(word.charAt(w));
        }

    }
    }
